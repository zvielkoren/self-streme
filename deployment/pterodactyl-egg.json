{
    "_comment": "Self-Streme Egg, based on https://github.com/pelican-eggs/eggs/blob/master/generic/nodejs/egg-node-js-generic.json",
    "meta": {
        "version": "PTDL_v2",
        "update_url": null
    },
    "exported_at": "2025-12-28T20:15:00+02:00",
    "name": "Self-Streme",
    "author": "zvielkoren@github.com",
    "description": "A powerful self-hosted streaming server with Stremio integration. Uses the project's pre-built Docker image for fast and reliable setup.",
    "features": null,
    "docker_images": {
        "Self-Streme (Latest)": "ghcr.io/zvielkoren/self-streme:latest"
    },
    "file_denylist": [],
    "startup": "node src/index.js",
    "config": {
        "files": "{\r\n    \.env": {\r\n        \"parser\": \"properties\",\r\n        \"find\": {\r\n            \"PORT\": \"{{server.build.default.port}}\",\r\n            \"TORRENT_PORT\": \"{{server.build.env.TORRENT_PORT}}\",\r\n            \"BASE_URL\": \"{{server.build.env.BASE_URL}}\",\r\n            \"TUNNEL_TOKEN\": \"{{server.build.env.TUNNEL_TOKEN}}\"\r\n        }\r\n    }\r\n}",
        "startup": "{\r\n    \"done\": \"Server running on port\"\r\n}",
        "logs": "{}",
        "stop": "^C"
    },
    "scripts": {
        "installation": {
            "script": "#!/bin/ash\n# Self-Streme Installation Script\n# Since we use a pre-built Docker image, this script only needs to copy files if the server directory is empty.\n\ncd /mnt/server\n\n# Pterodactyl's image mount can hide the /app contents.\n# If /mnt/server is empty, copy the app files from the image's /app directory.\nif [ ! -f package.json ]; then\n    echo \"No files found, copying from Docker image...\"\n    cp -r /app/.
fi\n\necho \"Installation check complete. Using pre-built image.\"",
            "container": "alpine:latest",
            "entrypoint": "ash"
        }
    },
    "variables": [
        {
            "name": "Torrent Port",
            "description": "Port for incoming P2P connections (TCP/UDP). Must be allocated as an additional port in the panel.",
            "env_variable": "TORRENT_PORT",
            "default_value": "6881",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|numeric|between:1024,65535",
            "field_type": "text"
        },
        {
            "name": "Base URL",
            "description": "The public URL for accessing Self-Streme (e.g., https://your-domain.com). Leave empty for auto-detection.",
            "env_variable": "BASE_URL",
            "default_value": "",
            "user_viewable": true,
            "user_editable": true,
            "rules": "nullable|string",
            "field_type": "text"
        },
        {
            "name": "Cloudflare Tunnel Token",
            "description": "Optional Cloudflare Tunnel token for exposing the server.",
            "env_variable": "TUNNEL_TOKEN",
            "default_value": "",
            "user_viewable": true,
            "user_editable": true,
            "rules": "nullable|string",
            "field_type": "text"
        }
    ]
}