{
    "_comment": "Self-Streme Egg, v4 - Standard Installation Flow",
    "meta": {
        "version": "PTDL_v2",
        "update_url": null
    },
    "exported_at": "2025-12-28T21:00:00+02:00",
    "name": "Self-Streme",
    "author": "zvielkoren@github.com",
    "description": "A powerful self-hosted streaming server with Stremio integration. This egg clones the repository and installs dependencies before starting.",
    "docker_images": {
        "Node.js 20": "ghcr.io/parkervcp/yolks:nodejs_20"
    },
    "file_denylist": [],
    "startup": "node src/index.js",
    "config": {
        "files": "{\".env\":{\"parser\":\"properties\",\"find\":{\"PORT\":\"{{server.build.default.port}}\",\"TORRENT_PORT\":\"{{server.build.env.TORRENT_PORT}}\",\"BASE_URL\":\"{{server.build.env.BASE_URL}}\",\"TUNNEL_TOKEN\":\"{{server.build.env.TUNNEL_TOKEN}}\"}}}",
        "startup": "{\"done\": \"Server running on port\"}",
        "logs": "{}",
        "stop": "^C"
    },
    "scripts": {
        "installation": {
            "script": "#!/bin/bash\n# Self-Streme Installation Script\n\ncd /mnt/server/\n\n# Install required system packages\napt update\napt install -y git curl\n\n# Define repository details\nGITHUB_REPO=\"https://github.com/zvielkoren/self-streme.git\"
BRANCH=\"main\"

# Clone or update the repository
if [ ! -d .git ]; then
    echo \"Cloning repository...\"
    git clone --single-branch --branch ${BRANCH} ${GITHUB_REPO} .
else
    echo \"Repository exists, updating...\"
    git fetch origin
    git reset --hard origin/${BRANCH}
fi
\n# Install Node.js dependencies\necho \"Installing dependencies...\"
npm ci --only=production\n\n# Create necessary directories\nmkdir -p media temp\n\necho \"Installation complete! Server is ready to start.\"",
            "container": "ghcr.io/parkervcp/yolks:nodejs_20",
            "entrypoint": "bash"
        }
    },
    "variables": [
        {
            "name": "Torrent Port",
            "description": "Port for incoming P2P connections (TCP/UDP). Must be allocated as an additional port in the panel.",
            "env_variable": "TORRENT_PORT",
            "default_value": "6881",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|numeric|between:1024,65535",
            "field_type": "text"
        },
        {
            "name": "Base URL",
            "description": "The public URL for accessing Self-Streme (e.g., https://your-domain.com). Leave empty for auto-detection.",
            "env_variable": "BASE_URL",
            "default_value": "",
            "user_viewable": true,
            "user_editable": true,
            "rules": "nullable|string",
            "field_type": "text"
        },
        {
            "name": "Cloudflare Tunnel Token",
            "description": "Optional Cloudflare Tunnel token for exposing the server.",
            "env_variable": "TUNNEL_TOKEN",
            "default_value": "",
            "user_viewable": true,
            "user_editable": true,
            "rules": "nullable|string",
            "field_type": "text"
        }
    ]
}