{
    "_comment": "Self-Streme Egg, v2",
    "meta": {
        "version": "PTDL_v2",
        "update_url": null
    },
    "exported_at": "2025-12-28T20:30:00+02:00",
    "name": "Self-Streme",
    "author": "zvielkoren@github.com",
    "description": "A powerful self-hosted streaming server with Stremio integration. Uses the project's pre-built Docker image for fast and reliable setup.",
    "docker_images": {
        "Self-Streme (Latest)": "ghcr.io/zvielkoren/self-streme:latest"
    },
    "file_denylist": [],
    "startup": "node src/index.js",
    "config": {
        "files": "{\".env\":{\"parser\":\"properties\",\"find\":{\"PORT\":\"{{server.build.default.port}}\",\"TORRENT_PORT\":\"{{server.build.env.TORRENT_PORT}}\",\"BASE_URL\":\"{{server.build.env.BASE_URL}}\",\"TUNNEL_TOKEN\":\"{{server.build.env.TUNNEL_TOKEN}}\"}}}",
        "startup": "{\"done\": \"Server running on port\"}",
        "logs": "{}",
        "stop": "^C"
    },
    "scripts": {
        "installation": {
            "script": "#!/bin/ash\n# Self-Streme Installation Script\n# This script ensures that if the server files are missing, they are copied from the image.\n\ncd /mnt/server\n\nif [ ! -f package.json ]; then\n    echo \"No files found, copying from Docker image's /app directory...\"\n    cp -r /app/.
fi\n\necho \"Installation check complete.\"",
            "container": "alpine:latest",
            "entrypoint": "ash"
        }
    },
    "variables": [
        {
            "name": "Torrent Port",
            "description": "Port for incoming P2P connections (TCP/UDP). Must be allocated as an additional port in the panel.",
            "env_variable": "TORRENT_PORT",
            "default_value": "6881",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|numeric|between:1024,65535",
            "field_type": "text"
        },
        {
            "name": "Base URL",
            "description": "The public URL for accessing Self-Streme (e.g., https://your-domain.com). Leave empty for auto-detection.",
            "env_variable": "BASE_URL",
            "default_value": "",
            "user_viewable": true,
            "user_editable": true,
            "rules": "nullable|string",
            "field_type": "text"
        },
        {
            "name": "Cloudflare Tunnel Token",
            "description": "Optional Cloudflare Tunnel token for exposing the server.",
            "env_variable": "TUNNEL_TOKEN",
            "default_value": "",
            "user_viewable": true,
            "user_editable": true,
            "rules": "nullable|string",
            "field_type": "text"
        }
    ]
}