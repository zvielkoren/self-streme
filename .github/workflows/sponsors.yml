name: Sponsor Management Automation

on:
  schedule:
    # Run daily at 00:00 UTC to check for sponsor changes
    - cron: "0 0 * * *"
  workflow_dispatch: # Allow manual trigger
  repository_dispatch:
    # Trigger via webhook for sponsor events
    types:
      - sponsor-created
      - sponsor-cancelled
      - sponsor-tier-changed

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  update-sponsors:
    name: Update Sponsors List
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch' || github.event_name == 'repository_dispatch'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: |
          npm install --save-dev @octokit/rest

      - name: Update sponsors list
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          node scripts/update-sponsors.js

      - name: Commit and push if changed
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add SPONSORS.md README.md
          git diff --quiet && git diff --staged --quiet || (git commit -m "chore: update sponsors list [skip ci]" && git push)

  welcome-sponsor:
    name: Welcome New Sponsor
    runs-on: ubuntu-latest
    if: github.event_name == 'repository_dispatch' && github.event.action == 'sponsor-created'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: |
          npm install --save-dev @octokit/rest

      - name: Send welcome message
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SPONSOR_LOGIN: ${{ github.event.client_payload.sponsor_login }}
          TIER_NAME: ${{ github.event.client_payload.tier_name }}
          MONTHLY_AMOUNT: ${{ github.event.client_payload.monthly_amount }}
        run: |
          node scripts/welcome-sponsor.js

  tier-changed:
    name: Handle Tier Change
    runs-on: ubuntu-latest
    if: github.event_name == 'repository_dispatch' && github.event.action == 'sponsor-tier-changed'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: |
          npm install --save-dev @octokit/rest

      - name: Handle tier change
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SPONSOR_LOGIN: ${{ github.event.client_payload.sponsor_login }}
          NEW_TIER: ${{ github.event.client_payload.new_tier }}
          OLD_TIER: ${{ github.event.client_payload.old_tier }}
        run: |
          node scripts/tier-changed.js

  cancelled-sponsor:
    name: Handle Sponsor Cancellation
    runs-on: ubuntu-latest
    if: github.event_name == 'repository_dispatch' && github.event.action == 'sponsor-cancelled'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: |
          npm install --save-dev @octokit/rest

      - name: Thank departed sponsor
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SPONSOR_LOGIN: ${{ github.event.client_payload.sponsor_login }}
          TIER_NAME: ${{ github.event.client_payload.tier_name }}
        run: |
          node scripts/cancelled-sponsor.js

  monthly-updates:
    name: Send Monthly Updates
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: |
          npm install --save-dev @octokit/rest

      - name: Check if first day of month
        id: check-date
        run: |
          if [ $(date +%d) = "01" ]; then
            echo "send_updates=true" >> $GITHUB_OUTPUT
          else
            echo "send_updates=false" >> $GITHUB_OUTPUT
          fi

      - name: Send monthly updates
        if: steps.check-date.outputs.send_updates == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          node scripts/monthly-updates.js

  sponsor-metrics:
    name: Generate Sponsor Metrics
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: |
          npm install --save-dev @octokit/rest

      - name: Generate metrics
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          node scripts/sponsor-metrics.js

      - name: Upload metrics artifact
        uses: actions/upload-artifact@v4
        with:
          name: sponsor-metrics
          path: metrics/sponsor-metrics.json
          retention-days: 90
